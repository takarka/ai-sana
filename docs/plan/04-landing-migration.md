# План работ: перенос лендинга `eighth-version/` в Angular (`apps/landing`)

**Задача.** Перенести публичный сайт `eighth-version/` (статический HTML/CSS/JS)
в `platform/front` как второе Nx-приложение `apps/landing` — Angular с
включённым SSR, официальной библиотекой `@angular/localize` для RU/KZ-перевода
вместо самодельного `js/i18n.js`, и вёрсткой на токенах дизайн-системы.

**Смежные документы.**
[01-bootstrap-platform.md](01-bootstrap-platform.md) — этот план предполагает,
что `platform/front` (Nx-воркспейс) уже создан по шагу 2 того плана; здесь
добавляется **второе** приложение в тот же воркспейс, `craft-web` не трогаем.
[03-design-system.md](03-design-system.md) — токены и компоненты, общие для
кабинета и лендинга, живут в `libs/shared/ui-tokens` и `libs/shared/ui`;
`landing` их переиспользует, а не заводит копию.
[ADR-0002](../adr/0002-frontend-stack-angular-tailwind.md) — стек фронтенда.
[ADR-0003](../adr/0003-landing-eighth-version-to-angular.md) — решение о переносе
и об удалении всех версий лендинга из root после cutover; закрывает открытый
вопрос L-1 (ниже оставлен как запись истории решения).
[ТЗ, FR-WEB-01…05](../tz/01-tz-platforma.md) — требования к публичному сайту,
FR-WEB-01 обновлён по [ADR-0003](../adr/0003-landing-eighth-version-to-angular.md).
[04-landing-migration-audit.md](04-landing-migration-audit.md) — аудит паритета
после переноса: что не доехало из `eighth-version` и что чинить в первую
очередь.

**Результат этапа.** `apps/landing` в `platform/front` — Angular-приложение с
SSR, четырьмя маршрутами (главная, MATRIX, BUILDER, PISA), полным переносом
контента и визуальных эффектов `eighth-version/`, переводом на `@angular/localize`
(RU — исходный язык, KZ — вторая локаль) и токенами из `libs/shared/ui-tokens`.
Статический `eighth-version/` перестаёт быть источником правды для публичного
сайта; он и все более ранние версии лендинга в root удаляются из репозитория
на шаге 6 (cutover), после подтверждения `apps/landing` на проде — см.
[ADR-0003](../adr/0003-landing-eighth-version-to-angular.md).

**Что этот этап НЕ делает:** не меняет `craft-web` и кабинет; не реализует
CRM-бэкенд для формы «Запросить демо» (FR-WEB-02) — на этом этапе форма только
валидируется и складывается в очередь-заглушку, интеграция с CRM — отдельная
задача; не добавляет третий язык (английский) — только готовит архитектуру под
него (NFR-I18N-05).

---

## 1. Итоговая структура каталогов

```
platform/front/
├── apps/
│   ├── craft-web/            # кабинет — без изменений
│   └── landing/               # ← создаётся на этом этапе
│       ├── src/
│       │   ├── app/
│       │   │   ├── pages/
│       │   │   │   ├── home/          # index.html
│       │   │   │   ├── matrix/        # matrix.html
│       │   │   │   ├── builder/       # builder.html
│       │   │   │   └── pisa/          # pisa.html
│       │   │   ├── shared/            # шапка, футер, lang-switcher, модалка
│       │   │   └── app.routes.ts
│       │   ├── locale/
│       │   │   ├── messages.xlf       # исходник RU (ng extract-i18n)
│       │   │   └── messages.kz.xlf    # перевод KZ
│       │   ├── main.ts
│       │   ├── main.server.ts
│       │   └── server.ts              # Express SSR-сервер (генерируется Nx/Angular)
│       └── project.json               # i18n-конфигурация локалей, SSR-таргеты
└── libs/shared/
    ├── ui-tokens/                      # переиспользуется (см. 03-design-system.md)
    └── ui/                             # переиспользуется, где применимо
```

---

## 2. Решения по стеку

| Решение | Значение | Обоснование |
|---|---|---|
| Приложение | второе Nx-приложение `landing` в существующем воркспейсе `platform/front` | Токены, ESLint-границы, CI-инфраструктура — общие с `craft-web`, дублировать воркспейс незачем |
| Генератор | `@nx/angular:application` с флагом SSR (`--ssr`) | Официальный Nx-генератор Angular-приложения с сервер-рендерингом «из коробки» |
| SSR | `@angular/ssr` (официальный пакет Angular), Express-сервер, гидратация (`provideClientHydration`) | Требование задачи; лендинг — публичная витрина, SEO и скорость первого рендера важны, в отличие от кабинета (см. 01-bootstrap-platform.md, п. 2.1: там SSR осознанно выключен) |
| Пререндер | точечно, для статических секций (`prerender` в конфиге SSR-билдера), где контент не персонализирован | Снижает нагрузку на Node-сервер; не отменяет SSR — используется как оптимизация поверх него, а не вместо |
| Перевод (i18n) | **`@angular/localize`** — официальная библиотека Angular | Прямое требование задачи («официальная библиотека ангулара для перевода»). Атрибуты `i18n` в шаблонах, извлечение строк `ng extract-i18n` → XLIFF, компиляция отдельных бандлов на каждую локаль |
| Локали | `ru` (source locale) + `kk-KZ` (перевод) | Соответствует существующему `js/i18n.js` (`ru`/`kz`) и NFR-I18N-01; архитектурно готово к третьему языку (NFR-I18N-05) добавлением локали в конфиг, без изменения кода |
| Роутинг локалей | `/ru/...` и `/kk/...` (`localize` billing в `angular.json`/`project.json`, `baseHref` на локаль) | Стандартный паттерн `@angular/localize`: каждая локаль — отдельный собранный бандл под своим префиксом. Лучше для SEO (индексируемые URL на каждый язык), чем текущее переключение без смены URL |
| Стили | Tailwind CSS v4 + `libs/shared/ui-tokens` | Тот же источник токенов, что и кабинет — см. [03-design-system.md, §4.5](03-design-system.md) |
| Тесты | Jest (unit) + Playwright (e2e, обе локали) | Совпадает с конвенцией `craft-web` |
| Хостинг SSR | Node-процесс (Express-сервер из `server.ts`) за реверс-прокси | Тот же паттерн, что для backend — уточняется в открытых вопросах (§7) |

### 2.1. Важное следствие выбора `@angular/localize`

В текущей реализации (`js/i18n.js`) язык переключается мгновенно, без перезагрузки
страницы (`applyTranslations()` по клику). `@angular/localize` — компиляция
**на этапе сборки**: переключение языка на лендинге станет переходом на другой
URL-префикс (`/ru/matrix` → `/kk/matrix`) с полной перезагрузкой страницы.

Это ожидаемое и допустимое поведение для витрины (в отличие от кабинета, где
`NFR-I18N-03` прямо запрещает потерю несохранённых данных при смене языка —
но это требование к `craft-web`, не к публичному сайту). Переключатель языка
(`.lang-btn`) на лендинге меняет `<a>`-ссылку на текущий путь в другой
локали, а не вызывает JS-функцию.

---

## 3. Инвентарь переноса контента

| Источник (`eighth-version/`) | Назначение в `apps/landing` |
|---|---|
| `index.html` | route `/` (`pages/home`) |
| `matrix.html` | route `/matrix` (`pages/matrix`) |
| `builder.html` | route `/builder` (`pages/builder`) |
| `pisa.html` | route `/pisa` (`pages/pisa`) |
| `styles.css` | разбирается на токены (→ `libs/shared/ui-tokens`, уже запланировано в 03-design-system.md) и компонентные стили страниц (→ `.scss` рядом с компонентами) |
| `js/core.js` | шапка, бургер-меню, модалка, аккордеон FAQ → Angular-компоненты в `app/shared/`, модалка — через Angular CDK `Overlay`/`Dialog` (тот же примитив, что и в `libs/shared/ui`, см. 03-design-system.md §5.1) |
| `js/i18n.js`, `js/lang/*.js` | **не переносится как код.** Ключи и переводы из словарей (`common.js`, `home.js`, `matrix.js`, `builder.js`, `pisa.js`) становятся источником для ручного переноса текста в шаблоны с атрибутами `i18n` (RU) и `messages.kz.xlf` (перевод) |
| `scene.js` (Three.js WebGL-сцена) | переносится как Angular-компонент, инициализация **только в браузере** (`afterNextRender` / `isPlatformBrowser`) — на сервере при SSR не выполняется |
| GSAP + ScrollTrigger (`matrix.html`) | аналогично: подключение и запуск только на клиенте, SSR отдаёт статичную разметку без анимации |
| Форма «Запросить демо» (FR-WEB-02) | Angular reactive form с валидацией; на этом этапе отправка — в очередь-заглушку (см. «Что этот этап НЕ делает») |
| Кнопка «Скачать презентацию» (FR-WEB-04) | статическая ссылка на PDF из `assets/`, версионирование через админку — не в этом этапе |

---

## 4. Пошаговый план

### Шаг 0. Предусловия

- [x] `platform/front` уже создан (шаг 2 плана [01](01-bootstrap-platform.md)) и в нём есть `apps/craft-web`.
- [x] Уточнить у заказчика конфликт с FR-WEB-01 (см. §7) до начала переноса контента —
      каркас приложения (шаги 1–2) можно готовить параллельно, перенос контента (шаг 3) — нет.
      Решено ADR-0003 до начала этого этапа (L-1 закрыт).

### Шаг 1. Генерация `apps/landing`

```bash
cd platform/front
npx nx g @nx/angular:application apps/landing \
  --ssr=true \
  --style=scss \
  --standalone=true \
  --routing=true \
  --unitTestRunner=jest \
  --e2eTestRunner=playwright \
  --linter=eslint \
  --tags=type:app,scope:landing
```

> Фактическая команда (Nx 21.6.11, см. примечание по версии в
> [01-bootstrap-platform.md, §2.3](01-bootstrap-platform.md)): каталог передаётся
> позиционным аргументом (`apps/landing`), а не через `--directory` — в этой версии
> генератора имя проекта берётся из последнего сегмента пути (`landing`), как и у
> `craft-web`; `--directory` в этом генераторе не поддерживается как отдельный флаг.

- [x] Проверить, что сгенерирован `server.ts` (Express) и таргет `serve-ssr`/`serve-static` в `project.json`.
      Фактически: `server.ts` + `main.server.ts` + `app.config.server.ts`/`app.routes.server.ts`
      сгенерированы; таргет называется `serve-static` (не `serve-ssr` — SSR в dev-режиме
      обслуживает сам `serve` через `@angular/build:dev-server`, `serve-static` — это
      раздача уже собранного `browser/` без SSR, для контейнера/CDN).
- [x] Подключить `libs/shared/ui-tokens` (Tailwind `@theme`, шрифты) — так же, как в `craft-web`.
      `stylePreprocessorOptions.includePaths` в `project.json` + `@use 'tailwindcss'; @use 'index' as tokens; @use 'theme.css';`
      в `styles.scss` — идентично `craft-web`.
- [x] Убрать демо-контент со стартовой страницы генератора (пустой layout с заголовком «CRAFT AI»,
      как и в `craft-web` на шаге 2 плана 01 — реальный контент лендинга переносится на шаге 3).

**DoD:**
```bash
npx nx build landing        # клиентский и серверный бандлы собираются — OK, плюс
                             # пререндер главного маршрута «из коробки»
npx nx serve landing        # SSR-сервер поднимается, HTML приходит с сервера — OK
                             # (curl http://localhost:4200/ содержит <h1>CRAFT AI</h1>,
                             # не пустой <app-root>)
```

### Шаг 2. `@angular/localize` и локали

```bash
npx nx g @angular/localize:ng-add --project=landing
```

> `nx g @angular/localize:init` из первой редакции плана не существует — генератор
> называется `ng-add` (`npx nx list @angular/localize` перечисляет фактические
> генераторы пакета). Добавляет `@angular/localize/init` в полифиллы проекта и
> `/// <reference types="@angular/localize" />` в `main.ts` — сам по себе локали
> не настраивает, это следующие пункты.

- [x] В `project.json` / `angular.json` прописать `i18n.sourceLocale: "ru"` и `i18n.locales.kk-KZ`.
      Фактически — не `baseHref`, а **`subPath`**: билдер (`@angular/build`) явно
      предупреждает, что `baseHref` в `i18n.sourceLocale`/`i18n.locales.*` «may
      lead to undefined behavior when used with SSR», и рекомендует `subPath`
      (задаёт и HTML `<base href>`, и имя выходной папки локали одним полем).
      `sourceLocale: { code: "ru", subPath: "ru" }`, `locales["kk-KZ"]: { translation: "...", subPath: "kk" }`
      → папки на выходе называются `ru`/`kk`, а не `ru`/`kk-KZ` (Angular обрезает
      код локали до `subPath`, если он задан).
- [x] `npx nx run landing:extract-i18n` → `src/locale/messages.xlf` (источник RU).
      По умолчанию `extract-i18n` пишет `messages.xlf` в корень воркспейса, а не
      в `src/locale/` — пришлось явно задать `options.outputPath` в таргете.
- [x] Скопировать в `messages.kk-KZ.xlf`, перенести переводы из `js/lang/*.js` (маппинг — таблица §3).
      На этом шаге перенесена только одна демонстрационная строка (`app.html`
      сейчас — временная заглушка из шага 1, не контент лендинга); полный перенос
      словарей `js/lang/*.js` — часть шага 3, когда появится реальная вёрстка,
      которую есть чем наполнять.
- [x] Настроить билд на обе локали (два бандла, `/ru` и `/kk`, `baseHref` на локаль).
      `localize: true` в конфигурации `production`; `nx build landing --localize`
      собирает `dist/apps/landing/browser/{ru,kk}` и `dist/apps/landing/server/{ru,kk}`
      с корректными `<base href>`/`<html lang>` в каждом.
- [x] Редирект с `/` на локаль по `Accept-Language` (или на `ru` по умолчанию) — в `server.ts` или на уровне реверс-прокси (см. открытый вопрос §7).
      Реализовано в `server.ts`: `GET /` смотрит `Accept-Language`, 302 на `/ru/`
      или `/kk/`. **Важная находка:** `@angular/ssr` по умолчанию отклоняет SSR
      живого рендеринга запросов, чей заголовок `Host` не входит в явный
      allowlist (защита от SSRF, добавлена в недавних версиях `@angular/ssr`) —
      без него `/ru/`/`/kk/` либо падают в CSR-заглушку, либо возвращают 400.
      Нужна переменная окружения `NG_ALLOWED_HOSTS` (список через запятую,
      **имя хоста без порта**, напр. `NG_ALLOWED_HOSTS=localhost` локально) — это
      реальный пункт для чек-листа деплоя, привязан к открытому вопросу L-2
      (хостинг SSR): без правильного значения на проде сайт будет отдавать
      пустой `<app-root>` или 400 на каждый запрос.
- [x] Переключатель языка (`app/shared/lang-switcher`) — ссылки на текущий путь в другой локали, `hreflang` в `<head>` на обеих версиях страницы.
      Каждая локаль — отдельный собранный бандл под своим `subPath`, общего
      клиентского роутера, знающего обе локали, нет — переключатель не роутит,
      а строит `<a href>` на текущий путь под другим префиксом через
      `document.location.pathname` (доступен и при SSR, и на клиенте).
      Чистая логика вынесена в `computeLangLinks()` и покрыта unit-тестами
      отдельно от Angular TestBed (мокать `document.location` в jsdom не
      получилось — свойство `location`/`location.pathname` не переопределяется).
      `hreflang` пока статический в `index.html` (один маршрут `/`); с
      добавлением маршрутов на шаге 3 должен стать per-route через `Meta`/`Title`.

**DoD:** `npx nx build landing --localize` собирает `dist/apps/landing/browser/{ru,kk}`
и соответствующие серверные бандлы — подтверждено. Страница на `/kk/...` отдаёт
переведённый HTML без JS (view-source) — подтверждено на собранном
`dist/apps/landing/server/server.mjs` (`NG_ALLOWED_HOSTS=localhost node dist/apps/landing/server/server.mjs`,
затем `curl localhost:4000/kk/`): `<html lang="kk-KZ">`, `<base href="/kk/">`,
переведённый текст присутствует в исходном HTML.

### Шаг 3. Перенос контента и компонентов

**Статус: сделано.** Каркас (шапка/футер/модалка/аккордеон), главная страница,
все три модульные страницы (MATRIX/BUILDER/PISA), сквозная WebGL-сцена,
текстовые ревилы и GSAP-скраб перенесены и проверены
(build/test/lint/stylelint/axe/e2e на обеих локалях); `coming-soon` как
временная заглушка для трёх страниц удалён — своей роли он больше не играет.
Ниже — по пунктам.

- [x] Компонент шапки/футера/бургер-меню (`app/shared/header`, `.../footer`) — из `core.js`.
      Бургер-меню — сигнал `navOpen`, порог 1180px. CTA-кнопка в шапке скрыта на
      мобильной ширине (нет места рядом с бургером) — но **обязательно** продублирована
      внутри самого выпадающего меню (`.nav__cta`): без этого дубля кнопка
      «Запросить демо» пропадала с телефона целиком, нашли на скриншоте, не на
      DoD-проверке — держите это в уме при добавлении новых пунктов меню.
- [x] Модалка на Angular CDK `Dialog` — сохранить контракт доступности лендинга (focus trap, `inert`, Escape, возврат фокуса — уже реализовано и проверено в `core.js`, компонент из `libs/shared/ui` при наличии, см. 03-design-system.md §4.1).
      `app/shared/demo-modal`; CDK дал focus trap/Escape/backdrop-click/возврат
      фокуса «из коробки» — переписывать логику `core.js` не пришлось. Кнопка —
      `CraftButton` из `libs/shared/ui`: тот же `.btn`, что и в кабинете, один
      источник стиля вместо копии.
- [x] Аккордеон FAQ — ARIA-разметка как на лендинге.
      `app/shared/faq-accordion` на `CdkAccordion`; `aria-expanded`/`aria-hidden`,
      как в `core.js`, состояние — через `CdkAccordionItem`, а не свой JS.
- [x] Главная страница (`home`) — вёрстка на токенах, тексты под `i18n`.
      Все 7 секций (герой, 3 модуля, дашборд-превью, эксперты, нормативная база,
      FAQ, финальный CTA); превью дашборда учителя переиспользует
      `CraftStatusBadge` из `libs/shared/ui` для статусов — те же цвет+иконка+текст,
      что и в кабинете. `.tag` из разметки исходника не имел объявленного CSS-стиля
      нигде в `styles.css` (проверено) — добавлен маленький стиль по аналогии
      с `.card__n`, а не оставлен неоформленным.
- [x] Страница MATRIX (`app/pages/matrix`) — герой, скраб-таймлайн урока
      («Самостоятельное прохождение» → «Безопасная практика с ИИ» →
      «Адресная помощь учителя») как кликабельный степпер (сигнал `activeStep`),
      блок стандартов ГОСО/ТУП (переиспользует общий `.mats`/`.mat` из
      `landing-layout.scss`). Скролл-скраб шагов добавлен отдельным проходом,
      см. пункт про GSAP ниже.
- [x] Страница BUILDER (`app/pages/builder`) — герой, два тезиса подхода,
      интерактивный тренажёр промпта (чипы критериев качества → `computed`
      промпт и оценка 15–100%, `lz__score`/`lz__verdict` реагируют на
      состояние), рельс из 4 модулей курса (`.chain__rail`), блок результатов.
      Раз локаль — отдельный собранный бандл `@angular/localize`, ветка
      `lang === 'kz'` из `page-builder.js` не нужна вовсе: `$localize`
      подставляет нужный текст на этапе сборки, а не в рантайме.
- [x] Страница PISA (`app/pages/pisa`) — герой, три уровня подготовки (архив,
      тренажёры по направлениям, спецмодуль PISA-2029), управленческий
      дашборд (3 карточки, график динамики готовности, список готовности по
      направлениям, блок рекомендации), юридический дисклеймер по товарному
      знаку OECD. График — **не** порт императивного `plotLine()` из
      `js/page-pisa.js` (там inline SVG рисуется через `document.createElementNS`
      с измерением `host.clientWidth` и перерисовкой по `resize`/смене языка):
      вместо этого геометрия (точки, путь, сетка) считается один раз в классе
      компонента на фиксированном `viewBox`, адаптивность — через CSS
      (`width: 100%` на `<svg>`), без слушателей и без JS на клиенте. Это
      соответствует требованию §4.3 плана — «статические SVG на данных с
      сервера, без анимации и интерактива сверх подсказки при наведении».
      Одну из подписей графика (`pisa.chart.label.4`) пришлось сократить в
      казахском переводе (с официального «PISA-2029 болжамы» до «2029
      болжамы») — при фиксированной ширине графика полная фраза перекрывала
      соседнюю подпись «3-тоқсан» (найдено скриншотом, не DoD-проверкой).
      Компонент `app/pages/coming-soon`, который раньше стоял на месте этих
      трёх маршрутов, удалён как более не нужный.
- [x] WebGL-сцена (`scene.js`) — перенесена в `app/shared/scene`. Шейдеры,
      геометрия и все числовые константы взяты дословно: сцена и есть картинка,
      сверено скриншотами со старым сайтом (герой, финальный CTA, мобильная
      ширина). Устройство:
      - `scene-globe.ts` — сам Three.js без Angular, грузится **только**
        динамическим `import()`, поэтому three уезжает в ленивый чанк
        (469kB raw / 99kB transfer) и не трогает initial-бандл;
      - `scene.ts` — компонент-обвязка, порт `bootScene()` из `core.js`:
        старт браузерный (`afterNextRender` + `requestIdleCallback`, SSR сцену
        не рендерит и TTI не занимает), плюс все отсечки оригинала —
        `prefers-reduced-motion` (в т.ч. включённый уже после старта),
        отсутствие WebGL, `saveData`, слабое устройство (12000 точек вместо
        24000 и меньший DPR), вкладка в фоне, `webglcontextlost` и пауза,
        когда на экране нет ни одной ink-полосы;
      - `.band--ink` стала прозрачной (фон ink-900 переехал на `body` в
        `styles.scss`) — именно сквозь эти полосы канвас и виден, как в
        оригинале; paper-полосы его закрывают;
      - `pages/home/hero-pins` — три выноски, приколотые к точкам сферы через
        `projectAnchor()` (перенос `PINS`/`updatePins()` из `page-home.js`).
      Два отличия от оригинала, оба вынужденные: `scene.js` отдавал наружу
      `resize()`, но никто не подписывал его на `window` — здесь слушатель
      есть; и ink-полосы приходится пересканировать после навигации, потому
      что здесь SPA, а не отдельная загрузка страницы.
- [x] Текстовые ревилы (`splitWords()`/`initReveals()` из `core.js`) — две
      атрибутные директивы в `app/shared/reveal`:
      - `appReveal` вешает `.js-rise` и по IntersectionObserver (порог 0.12,
        затем отписка) добавляет `.is-in` — блоки выезжают снизу, когда
        доезжают до вьюпорта. Расставлен ровно там, где в исходной разметке
        стоял `js-rise` (лиды героев, CTA, карточки, `.mat`/`.track`,
        дашборд, тренажёр, рельс, дисклеймер — количество на каждой странице
        сверено с оригиналом);
      - `appSplitWords` разбивает заголовок на слова (`.word-mask`/`.word`,
        задержка `--d` по 0.045с на слово) и добавляет `.js-reveal`.
      Разбивка правит DOM уже отрисованного заголовка — иначе никак: внутри
      `i18n`-блока текст один узел, и обернуть слова в шаблоне значит
      разломать перевод на два десятка строк. Делается один раз после
      гидратации; проверено, что Angular не ругается на расхождение DOM и что
      доступное имя заголовка не меняется (в дереве доступности слова
      по-прежнему разделены пробелами).
      **Отличие от оригинала.** До разбивки заголовок скрыт
      (`.js-split:not(.js-reveal)`): без этого SSR-текст успевает
      отрисоваться и через ~150мс (замерено) прыгает под маску — видно как
      мигание. В `eighth-version` этот момент прикрывал прелоадер, который
      сюда не переносился. Из-за того же SSR не сработал и `<noscript>` со
      стилем внутри — Angular экранирует его содержимое, и стиль
      отрисовывается как видимый текст на странице (поймано скриншотом);
      вместо него страховка сделана на `@media (scripting: none)`.
- [x] GSAP-сценарий (scroll-scrub на `matrix.html`) — `pages/matrix/matrix-scrub.ts`,
      порт `initHowScrub()` из `js/page-matrix.js`: `ScrollTrigger` на секции
      `#how` (`start: "top 30%"`, `end: "bottom 70%"`) переключает шаги по
      прогрессу прокрутки, `gsap.matchMedia()` включает это только от 1024px.
      Клик работает всегда и на любой ширине — скраб именно надстройка.
      gsap грузится динамическим `import()` (свой ленивый чанк, 112.53kB raw /
      40.16kB transfer) и только если пользователь не просил меньше движения.
      Проверено: на 1440px прогресс делится на трети (0→1→2 ровно как
      `Math.floor(progress * 3)` в оригинале), на 720px скраба нет, при
      reduced-motion чанк не запрашивается.
      Побочный плюс: в оригинале gsap подключался с CDN и при недоступности
      CDN скраб молча не работал (ровно это и наблюдается на локальной копии
      старого сайта в песочнице) — здесь он в бандле и от сети не зависит.
- [x] Форма «Запросить демо» — reactive form, клиентская и серверная валидация полей (ФИО, школа, e-mail, телефон), защита от спама (honeypot-поле минимум) — см. FR-WEB-02.
      `demo-modal.ts`: `FormBuilder` + `Validators`, honeypot-поле `website`
      (визуально скрыто и `aria-hidden`, не `display:none` — простые боты часто
      игнорируют это отличие). Интеграции с CRM по-прежнему нет (см. «что этот
      этап не делает» плана) — заявка просто помечается принятой.
- [ ] Meta-теги (`Title`, `Meta` из `@angular/platform-browser`) на каждый маршрут и на каждую локаль; `sitemap.xml` и `robots.txt` в `public/`. — не сделано на этом проходе.

**Найденный и исправленный по ходу баг с контрастом.** `.nav a` в шапке
использует `--on-ink-dim` — токен, само по себе проходящий AA на сплошном
`--ink-900` (7.9:1). Но шапка — `position: sticky` с полупрозрачным фоном
(`color-mix(..., 76%, transparent)`) поверх контента, а не его предок: когда
под шапкой прокручивается paper-секция, эффективный фон светлеет настолько,
что контраст `--on-ink-dim` падает до 4.06:1 (не проходит 4.5:1). Поймано
автоматической проверкой axe (`libs/shared/ui/tools/check-a11y.mjs`-подобный
скрипт против собранного `dist/apps/landing/server/server.mjs`), не глазами.
Исправлено повышением непрозрачности до 90% (проверено: 6.49:1 даже над paper) —
без реализации отдельного анимированного состояния `is-stuck`, которое было
осознанно не перенесено в начале шага, но как раз и решало эту проблему в
оригинале другим способом (переход .76 → .94 при скролле).

**Второй найденный и исправленный контраст-баг** (та же природа: токен цвета
достаточно контрастен для одного применения, но не для того, куда его
поставили). `--lvl-low` (`#d0503c`) — часть троицы `--lvl-high/mid/low`,
изначально созданной для иконки в `CraftStatusBadge` на `--surface`
(шаг 2.5). Здесь эти токены попытались использовать как цвет **текста**
на paper-фоне: `builder.lz__score` (индикатор качества промпта) и
`pisa.hard__pct` (проценты готовности по направлениям). axe против собранного
`server.mjs` дал 4.29:1 — ниже требуемых AA 4.5:1 для обычного текста.
Исправлено тем же приёмом, что и в `CraftStatusBadge`: текст остаётся `--fg`,
а уровень передаёт отдельный цветной индикатор (кружок перед текстом в
`.lz__score`, заливка `.meter i` в `.hard`) — не сам текст.

**DoD:** визуальная и функциональная эквивалентность с `eighth-version/`
подтверждена для каркаса, главной страницы и всех трёх модульных страниц
(Playwright-скриншоты на обеих локалях, `docs/plan/`-сессия). axe A/AA —
чисто на всех собранных страницах (`ru`/`kk`, включая открытую модалку и
интерактивные состояния тренажёра BUILDER) — проверено тем же способом, что
и в `libs/shared/ui` (шаг 2.5). WebGL-сцена, ревилы и GSAP-скраб перенесены
отдельными проходами (см. их пункты выше). Lighthouse SEO не запускался на
этом проходе.

### Шаг 4. Доступность и качество

**Статус: сделано** (кроме пункта про WebGL — см. ниже).

- [x] axe без нарушений уровня A/AA на всех четырёх страницах, обеих локалях.
      Формализовано в `apps/landing/tools/check-a11y.mjs` (по образцу
      `libs/shared/ui/tools/check-a11y.mjs`, но против реального SSR-сервера,
      а не статического Storybook — маршруты и baseHref завязаны на
      `@angular/ssr`). Проверяет 10 состояний: home/matrix/builder/pisa ×
      ru/kk, плюс открытая модалка демо на обеих локалях. Скрипт сам поднимает
      и глушит `server.mjs` — `nx run landing:build:production --localize &&
      node apps/landing/tools/check-a11y.mjs`.
- [x] Клавиатурная навигация (Tab-порядок, фокус-видимость) — ручной прогон.
      Прогнан вручную через Playwright, управляющий реальными нажатиями Tab
      (не assertion-набор, а разведка — как и написано в плане). Нашёл три
      реальных бага, не видных по чек-листу DoD:
      1. **Мобильное меню теряло фокус при открытии.** Ниже 1180px `.nav` —
         `position: fixed`-дропдаун, скрытый в закрытом состоянии через
         `visibility: hidden` (сам по себе корректно выпадает из tab-order).
         Но в DOM `<nav>` стоит раньше бургера — открыв меню (мышью или
         Enter/Space на бургере), следующий Tab уходил вперёд мимо уже
         видимой навигации сразу в контент страницы: видимый и фокусируемый
         порядок расходились (WCAG 2.4.3). Исправлено переносом фокуса на
         первый пункт меню при открытии (`header.ts: toggleNav()`) и
         добавлением Escape для закрытия с возвратом фокуса на бургер — тот
         же контракт, что уже был у демо-модалки на CDK Dialog.
      2. **`transition: all` анимировал фокус-рамку вместо мгновенного
         появления.** `.chip` (фильтры дашборда учителя) и `.lz-chip`
         (тренажёр промпта BUILDER) объявляли `transition: all 0.25s` для
         плавной смены фона/цвета по hover/`.is-on`, но `all` заодно ловил
         `outline` из глобального `:focus-visible` (`base/_a11y.scss`) —
         фокус-рамка на Tab не появлялась мгновенно, а 250мс наплывала из
         почти невидимого состояния. Исправлено сужением `transition` до
         конкретных цветовых свойств (`background-color`/`border-color`/
         `color`) в обоих компонентах.
      3. Отсутствовал класс `.grid-2` — де-факто найден и исправлен ещё на
         шаге 3 (см. выше), здесь не переоткрывается.
      Тексты полей ввода демо-модалки и `.desk__search` используют смену
      `border-color` на `:focus-visible` вместо `outline` — сознательное
      решение из шага 3, оставлено как есть (это легитимная техника, а не
      баг: контраст 1px рамки не хуже, чем у соседних элементов).
- [x] Playwright e2e: переключение языка, отправка формы демо, открытие/закрытие модалки, работа при масштабе 200%.
      `apps/landing-e2e/` — только Chromium (`/opt/pw-browsers/chromium`,
      единственный предустановленный браузер в этом окружении; firefox/webkit
      не ставились и не нужны для этого прохода). `webServer` поднимает не
      `landing:serve` (dev-server не умеет отдавать обе локали по
      `subPath`), а `nx run landing:build:production --localize` +
      `server.mjs` — тот же способ, каким страницы проверялись вручную на
      шагах 3–4. 15 тестов: `language-switch.spec.ts` (переход ru↔kk с
      сохранением пути, реальный казахский текст на странице, а не ru-заглушка),
      `demo-modal.spec.ts` (открытие с фокус-ловушкой, возврат фокуса,
      закрытие по Escape/клику по подложке, отклонение неполной формы,
      успешная отправка, тихий honeypot), `mobile-nav-keyboard.spec.ts`
      (регрессия на баг №1 выше), `reflow-200.spec.ts` (640px — эквивалент
      200%-масштаба на типичном 1280px desktop: нет горизонтальной
      прокрутки на всех четырёх страницах, бургер-меню и форма демо
      остаются рабочими).
- [x] Бюджет производительности: проверить, что WebGL-сцена не блокирует TTI (ленивая инициализация после первого рендера).
      Выполнено вместе с переносом сцены (см. её пункт в шаге 3). Ленивость
      обеспечена не «на словах», а структурно: three виден сборщику только
      через динамический `import()`, поэтому уезжает в отдельный чанк
      (`scene-globe`, 469.33kB raw / 98.59kB transfer), а запрашивается он ещё
      и не сразу — из `afterNextRender` + `requestIdleCallback`, то есть после
      первой отрисовки. Initial-бандл от сцены почти не вырос: 573.96kB raw /
      146.30kB transfer против 564.87kB / 143.21kB до неё, в рамках
      650kb-warning/1mb-error (project.json, `production`). Проверено, что при
      `prefers-reduced-motion` и без WebGL чанк сцены не запрашивается вовсе —
      по сетевым запросам, а не по виду страницы.

**DoD:** все пункты шага выполнены и автоматизированы там, где это осмысленно
(axe и e2e — постоянные скрипты/тесты в репозитории, а не разовые прогоны).

### Шаг 5. CI

- [ ] `.github/workflows/platform-front.yml` (или отдельный workflow) расширяется на `nx affected -t lint,test,build` для `landing`.
- [ ] Отдельный e2e-джоб Playwright на `landing` (обе локали).
- [ ] Lighthouse CI на `/ru` и `/kk` с порогом по SEO/Performance/Accessibility.

**DoD:** пайплайн зелёный на pull request, затрагивающем `apps/landing`.

### Шаг 6. Точка перехода (cutover) и удаление старых версий лендинга

- [ ] `apps/landing` разворачивается на стейджинге, параллельно с текущим статическим `eighth-version/`.
- [ ] Сверка контента и форм с заказчиком/методистом.
- [ ] Убедиться, что весь нужный материал уже перенесён: токены и обоснование контраста
      из `eighth-version/DESIGN.md` — в [03-design-system.md](03-design-system.md);
      переводы из `js/lang/*.js` и `*/i18n.js` каждой версии — в `apps/landing/src/locale/*.xlf`
      (см. §3 выше). После шага 6 эти файлы недоступны иначе, чем через git-историю.
- [ ] Домен/прокси переключается на SSR-сервер `landing`.
- [ ] Удалить из root **все** версии лендинга, по решению [ADR-0003](../adr/0003-landing-eighth-version-to-angular.md):
  - [ ] папки `second-version/`, `third-version/`, `fourth-version/`, `fifth-version/`,
        `sixth-version/`, `seventh-version/`, `eighth-version/`;
  - [ ] лендинг-файлы без версии прямо в root: `index.html`, `app.js`, `styles.css`, `i18n.js`, `hero-1440.png`.
  - [ ] `requrement` (исходное текстовое ТЗ на лендинг) — не версия сайта, а документ;
        оставить в root либо перенести в `docs/`, решение — отдельным мелким коммитом,
        не блокирует cutover.
- [ ] Один коммит `chore(landing): remove legacy landing versions, superseded by apps/landing`
      — не смешивать с кодовыми изменениями `apps/landing`.

**DoD:** публичный сайт обслуживается `apps/landing`; в root не осталось ни
`eighth-version/`, ни более ранних версий (`second-version` … `seventh-version`),
ни лендинг-файлов без версии; `git log` по удалённым путям сохраняет историю.

---

## 5. Соглашения

| Что | Правило |
|---|---|
| Компоненты | Standalone, `OnPush`, сигналы — как в `craft-web` (01-bootstrap-platform.md, §5) |
| Тексты в шаблонах | Только через `i18n`/`i18n-*` атрибуты, ни одной «зашитой» строки — соответствует духу NFR-I18N-02, распространяем и на лендинг |
| Токены | Только `libs/shared/ui-tokens`, hex вне токенов запрещён (тот же Stylelint, что у `craft-web`) |
| Ветки | `feature/landing-<кратко>` от ветки разработки |
| Коммиты | Conventional Commits (`feat(landing): ...`) |

---

## 6. Definition of Done этапа

1. `apps/landing` собирается, проходит `lint`/`test`/`build` для обеих локалей.
2. SSR отдаёт полностью отрендеренный HTML (проверено view-source, без JS).
3. Контент всех четырёх страниц перенесён и эквивалентен `eighth-version/`.
4. Перевод — через `@angular/localize`, ключи `js/lang/*.js` перенесены в XLIFF, ни одной строки мимо `i18n`.
5. axe (A/AA) и Playwright e2e — зелёные на обеих локалях.
6. CI-пайплайн для `landing` зелёный на pull request.
7. Cutover выполнен: прод обслуживается `apps/landing`; `eighth-version/` и все
   более ранние версии лендинга удалены из root (ADR-0003).

**Оценка трудоёмкости:** 8–12 рабочих дней для одного фронтенд-инженера — перенос
четырёх страниц с WebGL/GSAP-эффектами и двуязычным SSR ощутимо больше, чем
пустой каркас `craft-web` (01-bootstrap-platform.md давал 3–5 дней на голый скелет
без контента).

---

## 7. Открытые вопросы

| № | Вопрос | Рекомендация |
|---|---|---|
| L-1 | ~~Конфликт с FR-WEB-01~~ — **решено**, см. [ADR-0003](../adr/0003-landing-eighth-version-to-angular.md): FR-WEB-01 обновлён, реализация переезжает на `apps/landing`, все версии лендинга в root удаляются после cutover (шаг 6) | — |
| L-2 | Хостинг Node-SSR: тот же контур, что backend (.NET/Aspire), или отдельный (например, Vercel/статический CDN + edge-функции)? | Требует решения по инфраструктуре — вне рамок этого плана, зависит от DevOps-стратегии платформы |
| L-3 | Определение локали на `/`: по `Accept-Language`, по гео (RU/KZ регион) или всегда редирект на `ru`? | По умолчанию `ru` с редиректом при явном казахском `Accept-Language` — проще всего проверить и не даёт «прыгающего» UX |
| L-4 | Пререндер (SSG) вместо SSR для полностью статичных секций (FAQ, О продукте)? | Не блокирует этот этап: `prerender` — опция поверх уже настроенного SSR-билдера, можно включить точечно после шага 3 |
| L-5 | Кто переводит контент на казахский (шаг 2)? Текущие `js/lang/*.js` уже содержат готовый перевод — переносится он или проверяется заново методистом? | Перенести существующий перевод как есть на этом этапе; вычитку методистом — отдельной задачей, не блокирует cutover |

---

## 8. Связь с ТЗ

| Элемент этапа | Требования, которые он закрывает |
|---|---|
| SSR + `apps/landing` | FR-WEB-01 (в редакции по [ADR-0003](../adr/0003-landing-eighth-version-to-angular.md)), NFR-I18N-01 |
| `@angular/localize`, локали `ru`/`kk-KZ` | NFR-I18N-01, NFR-I18N-02, NFR-I18N-05 |
| Токены из `libs/shared/ui-tokens` | FR-WEB-03 |
| Форма «Запросить демо» (каркас) | FR-WEB-02 (без интеграции с CRM) |
| Кнопка «Скачать презентацию» | FR-WEB-04 |
| axe A/AA, клавиатурная навигация | NFR-A11Y-* (общие требования доступности) |
